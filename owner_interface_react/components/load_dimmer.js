import React from 'react'

const LoadDimmer = (props) => {
    return (
        <div className="loader-shade">
            <i className="material-icons loader-shade-spinner">sync</i>
        </div>
    );
}

export default LoadDimmer
